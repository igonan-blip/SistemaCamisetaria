import { useEffect,useState } from "react"; import { useAuth } from "@/contexts/AuthContext"; import { getPedidos,getEtapas,money } from "@/lib/db";
export function Dashboard(){const {profile}=useAuth();const [pedidos,setPedidos]=useState<any[]>([]),[etapas,setEtapas]=useState<any[]>([]);useEffect(()=>{Promise.all([getPedidos(),getEtapas()]).then(([p,e])=>{setPedidos(p);setEtapas(e)})},[]);const total=pedidos.reduce((s,p)=>s+Number(p.valor_total),0),pago=pedidos.filter(p=>p.status_pagamento==="pago").length;return <div className="space-y-6"><div><p className="text-sm text-text-600">Bem-vindo(a){profile?`, ${profile.nome.split(" ")[0]}`:""}.</p><h2 className="text-2xl">Painel</h2></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="card p-4"><p className="text-xs text-text-500">Pedidos</p><strong className="text-2xl">{pedidos.length}</strong></div><div className="card p-4"><p className="text-xs text-text-500">Em produção</p><strong className="text-2xl">{pedidos.filter(p=>p.etapas_producao?.slug!=="entregue").length}</strong></div><div className="card p-4"><p className="text-xs text-text-500">Valor dos pedidos</p><strong className="text-2xl">{money(total)}</strong></div><div className="card p-4"><p className="text-xs text-text-500">Pagos</p><strong className="text-2xl">{pago}</strong></div></div><div className="card p-5"><h3 className="mb-4">Pedidos por etapa</h3><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{etapas.map(e=><div key={e.id} className="border rounded-tag p-3"><div className="text-sm">{e.nome}</div><div className="text-xl font-semibold">{pedidos.filter(p=>p.etapa_id===e.id).length}</div></div>)}</div></div></div>}